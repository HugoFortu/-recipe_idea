<div class="recipe-banner" style="background-image: url('<%= @recipe.image_url %>')"></div>
<div class="container d-flex align-items-center">
  <h3><%= @recipe.name %></h3>
  <% if !current_user.nil? %>
    <p id="button-add">Ajouter cette recette à ma liste de recettes</p>
    <%= button_to user_recipes_path(recipe: @recipe), class: "btn px-2 align-items-center" do %>
      <%= image_tag 'plus.png', class:"plus" %>
    <% end %>
  <% end %>
</div>

<div class="d-flex row ingredient-container">
  <% @ingredients.each do |ingredient|%>
  <div class="col-3 text-center card-ingredient">
    <p><%= "#{ingredient.ingredient.name}" %></p>
    <%= image_tag ingredient.ingredient.image, class: "list-ingredients mt-2" %>
    <p><%= "Doses: #{ingredient.dose}" %></p>
  </div>
  <% end %>
</div>





  <% @steps.each do |step|%>
    <div class="container">
      <p><%= "#{step.order}- #{step.description}" %></p>

    </div>
  <% end %>

